//
// Created by wbzhang on 2020/9/20.
//

#include <iostream>
#include <vector>

using namespace std;

// 分割数字
void splitNum(int &x,vector<int> &sp)
{

};

// 判断是否相等

// A不能为0，其他的可以为0，并且全不相同...满足AB+CD=EF

int main()
{
	int M,N;
	cin>>M >>N;

	int ans = 0;
	vector<int> sp;
	int x = M;
	while(x<=N){
		splitNum(x,sp);
		if(sp[0]!=sp[])

		x++;
	}


	return 0;
}